{
  "scripts": {
    "test": "echo 0",
    "build": "npm-run-all build:web build:assets",
    "build:assets": "cpx 'src/assets/**/*.*' dist/assets",
    "build:web": "parcel build src/index.html --public-url=.",
    "precommit": "pretty-quick --staged",
    "start": "live-server --open=src/index.html"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "husky": "^1.0.0",
    "live-server": "^1.2.0",
    "npm-run-all": "^4.1.3",
    "parcel-bundler": "^1.10.0",
    "prettier": "^1.14.3",
    "pretty-quick": "^1.7.0"
  },
  "dependencies": {
    "mini.css": "^3.0.0"
  }
}
